## Manual Exposures

<Warning>Manual logging is error-prone and can introduce issues like uneven exposure, which compromise experiments.</Warning>

You can query your gates/experiments without triggering an exposure, and manually log the exposures later: